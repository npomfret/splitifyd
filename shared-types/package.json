{
  "name": "@bill-splitter/shared-types",
  "version": "1.0.0",
  "description": "Shared type definitions for Bill Splitter webapp and Firebase functions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
  }
}