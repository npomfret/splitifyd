// Re-export from new field-registry module (single source of truth)
export { buildBrandingTokensFromForm, EMPTY_TENANT_DATA, extractFormDataFromTokens, validateTenantData } from './field-registry';

// Re-export types
export * from './types';

// Re-export components
export { AutoSection } from './components';
