{
  "name": "backend",
  "version": "1.0.0",
  "description": "Firebase backend system with Cloud Functions for storing and retrieving JSON documents",
  "scripts": {
    "build:scripts": "tsc --project tsconfig.build.json",
    "build": "npm run build:check && npm run build:scripts && cd functions && npm run build",
    "build:check": "tsc --project scripts/tsconfig.json --noEmit",
    "clean": "tsx scripts/kill-emulators.ts && rm -rf firebase-export-* *.log functions/*.log ../*.log && rm -rf public",
    "super-clean": "npm run clean && rm -rf node_modules",
    "kill-emulators": "tsx scripts/kill-emulators.ts",
    "switch-instance": "tsx scripts/switch-instance.ts",
    "test": "ORIGINAL_TEST_SCRIPT=test node ../scripts/test-wrapper.js",
    "test:unit": "ORIGINAL_TEST_SCRIPT=test:unit node ../scripts/test-wrapper.js",
    "test:integration": "ORIGINAL_TEST_SCRIPT=test:integration node ../scripts/test-wrapper.js",
    "link-webapp": "rm -rf public && ln -sf ../webapp-v2/dist public",
    "start-emulators": "npm run kill-emulators && tsx scripts/generate-firebase-config.ts && export GCLOUD_PROJECT=splitifyd && tsx scripts/start-with-data.ts",
    "delete-firebase-data": "cd functions && node scripts/delete-firebase-data.js",
    "deploy": "echo 'Use deploy:prod for production deployment'",
    "deploy:prod": "tsx scripts/switch-instance.ts prod && cd .. && NODE_ENV=production npm run build && cd firebase && firebase deploy --only functions,firestore:rules,hosting && rm functions/.env",
    "deploy:functions": "tsx scripts/switch-instance.ts prod && firebase deploy --only functions && rm functions/.env",
    "deploy:hosting": "cd .. && NODE_ENV=production npm run build && cd firebase && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:indexes": "firebase deploy --only firestore:indexes --force",
    "seed-policies:prod": "tsx scripts/switch-instance.ts prod && tsx scripts/seed-policies.ts production && rm functions/.env",
    "fix-users": "tsx scripts/fix-broken-users.ts emulator",
    "fix-users:prod": "tsx scripts/switch-instance.ts prod && tsx scripts/fix-broken-users.ts production && rm functions/.env",
    "paginate-users": "tsx scripts/paginate-users.ts emulator",
    "paginate-users:prod": "tsx scripts/switch-instance.ts prod && tsx scripts/paginate-users.ts production && rm functions/.env",
    "logs": "firebase functions:log",
    "manage-functions": "tsx scripts/manage-deployed-functions.ts",
    "analyze-performance": "npx tsx scripts/analyze-function-performance.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.14",
    "dotenv": "^17.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "xss": "^1.0.15"
  }
}
